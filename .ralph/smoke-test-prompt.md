Smoke test mode.

Append exactly one line to `/Users/chris/workspace/sigmulhae/.ralph/progress.md` in this format:

`[SMOKE TEST] opencode write path works`

Do not modify any other files.
Then reply with exactly:

`SMOKE_OK`
